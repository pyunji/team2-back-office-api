<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.webapp.dao.db1member.AccountDao">
	<select id="selectMemberByMid" parameterType="String" resultType="com.mycompany.webapp.vo.Member">
		select * from MEMBER where mid = #{mid}
	</select>
	

</mapper>